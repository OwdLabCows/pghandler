CREATE TABLE test_basic_sql_query_foo(
    id SERIAL,
    comment text,
    PRIMARY KEY(id)
);

CREATE TABLE test_basic_sql_query_bar(
    id SERIAL,
    comment text,
    PRIMARY KEY(id)
);

CREATE TABLE table4test_empty(
    id SERIAL,
    comment text,
    PRIMARY KEY(id)
);

CREATE TABLE test_returntype_dict(
    id SERIAL,
    comment text,
    PRIMARY KEY(id)
);